#include <bits/stdc++.h>

using namespace std;


// as fast as kmp
class Solution {
public:
    int strStr(string haystack, string needle) {
        return haystack.find(needle);
    }
};

//KMP


int main(){
	
}
